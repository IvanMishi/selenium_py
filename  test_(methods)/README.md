# Основные методы Selenium

## Инженер по тестированию переходит в директорию, предназначенную для хранения файлов с тестами
```
cd selenium_tests
```
## Из директории selenium_tests активирует виртуальное окружение
```sh
source selenium_env/bin/activate
```
## В активированном окружении запускает тест 
```sh
python3  test_(methods)/test_A.py
```
## Для вывода результата в отдельный файл зпускает командой 
```sh
python3 test_(methods)/test_A.py >> test_A2/output.txt
```
Где test_A.py -  скрипт с тестом, а output.txt - файл, в который будет записан вывод теста.

Selenium предлагает множество методов, большинство из которых будут упомянуты здесь.

### Навигация по истории браузера
```driver.back()```
Возвращает на предыдущую страницу, аналогично нажатию кнопки "назад".
```driver.forward()```
Перемещает вперёд по истории, как кнопка "вперёд".
```driver.refresh()``` 
Обновляет текущую страницу, имитируя нажатие кнопки обновления.

### Работа со скриншотами
```driver.get_screenshot_as_file("../file_name.jpg")``` 
Сохраняет скриншот по указанному пути и возвращает True при успехе.
```driver.save_screenshot("file_name.jpg")``` 
Сохраняет скриншот в папке проекта.
```driver.screenshot('file_name.png')```
Возвращает скриншот в виде двоичных данных для дальнейшей обработки.
```driver.get_screenshot_as_base64()``` 
Возвращает скриншот в кодировке Base64, удобен для встроенных изображений.

### Открытие и закрытие страниц и браузера
```driver.get("http://example_url.ru")```
Открывает указанный URL в браузере.
```driver.quit()```
Закрывает все вкладки и окна, завершает процесс драйвера, освобождает ресурсы.
```driver.close()```
Закрывает только текущую вкладку.

### Исполнение JavaScript 
```driver.execute_script("script_code")```
Выполняет JavaScript код на текущей странице.
```driver.execute_async_script("script_code" , *args )```
Асинхронно выполняет JavaScript код. 
Удобно для работы с AJAX и промисами.

### Время ожидания
```driver.set_page_load_timeout()```
Устанавливает таймаут на загрузку страницы. Выбрасывает исключение, если время вышло.

### Работа с окном браузера
```driver.get_window_position()```
Возвращает текущие координаты окна в виде словаря, например, {'x': 10, 'y': 50}.
```driver.maximize_window()```
Разворачивает окно на весь экран.
```driver.minimize_window()```
Сворачивает окно.
```driver.fullscreen_window()```  
Переводит окно в полноэкранный режим.
```driver.get_window_size()```
Возвращает размеры окна как словарь, например, {'width': 945, 'height': 1020}.
```driver.set_window_size(800,600)```
Устанавливает новый размер окна.

### Работа с элементами
```element.click()```
Симулирует клик по элементу.

```element.send_keys("text")```
Вводит текст в текстовое поле.
```element.clear()```
Очищает текстовое поле.
```element.is_displayed()```
Проверяет, отображается ли элемент на странице.
```element.is_enabled()```
Проверяет, доступен ли элемент для взаимодействия (например, не заблокирован).
```element.is_selected()```
Проверяет, выбран ли элемент (актуально для радиокнопок и чекбоксов).
```element.get_attribute("attribute")```
Возвращает значение указанного атрибута элемента.
```element.text```
Возвращает текст элемента.
```element.submit()```
Отправляет форму, в которой находится элемент.

### Фреймы
```driver.switch_to.frame("frame_name")```
Переключает фокус на указанный фрейм.
```driver.switch_to.default_content()```
Возвращает фокус на основное содержимое страницы, выходя из фрейма.


## Тестовые данные test_A0
- [x] Незарегестированный пользователь переходит по ссылке.
- [x] Ищет нопку на странице.
- [x] Выполняет клик по найденой кнопкенажимает на неё.

#### В программе использует:
 ```.click()``` Симулирует клик по элементу.


## Тестовые данные test_A01
- [x] Незарегестированный пользователь переходит по ссылке.
- [x] Ищет текстовое поле на странице.
- [x] Заполяет найденное поле поьзовательскими данными.
- [x] Ищет кнопку и выполняет по ней клик.
#### В программе использует:
```.send_keys('text')``` Вводит текст в текстовое поле.
 ```.click()``` Симулирует клик по элементу.

## Тестовые данные test_A02
- [x] Незарегестированный пользователь переходит по ссылке.
- [x] Ищет нопку на странице для начала теста и нажимает на неее.
- [x] Ищет элемент с данными м сохраняет текст в переменную.
- [x] 'Ищет поле для ввода текста на веб-странице и заполняет данными из переменной.
- [x] 'Ищет кнопку проверки кода на веб-странице и нажимает на нее.

#### В программе использует:
```.click()``` Симулирует клик по элементу.
```.text``` Возвращает текст элемента.
```.send_keys('text')``` Вводит текст в текстовое поле.


## Тестовые данные test_A
- [x] Незарегестированный пользователь переходит по ссылке.
- [x] Создает переменную с ожидаемым результатом.
- [x] Ищет поле ввода текста на странице.
- [x] Заполняет поле текстом.
- [x] Находит кнопку для отправки формы и нажимает на неё.
- [x] Получает алерт, выводит из него текст в консоль в качестве ответ и убеждается что этот текст соответствует ожидаемому.
#### В программе использует:
```.send_keys()``` Вводит текст в текстовое поле.
 ```.click()``` Симулирует клик по элементу.


## Тестовые данные test_B
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Переносит числа из серых полей в синие.
- [x] Нажимает кнопку "Проверить"
- [x] Выводит текст из появившегося элемента с id="congrats" в качестве ответа в консоль.
#### В программе использует:
```.text``` Возвращает текст элемента.
```.send_keys()``` Вводит текст в текстовое поле.
```.clear()```  Очищает текстовое поле.
```.click()``` Симулирует клик по элементу.

## Тестовые данные test_C
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет поля для ввода текста.
- [x] Проходит по каждому полю и очищает его.
- [x] Ищет кнопку для проверки результата и нажимает на нее.
- [x] Получает алерт, выводит из него текст в консоль в качестве ответа.
#### В программе использует:
```.clear()```  Очищает текстовое поле.

## Тестовые данные test_D
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет поля для ввода текста.
- [x] Проходит по каждому полю и очищает его если элемент не отключен.
- [x] Ищет кнопку для проверки результата и нажимает на нее.
- [x] Получает алерт, выводит из него текст в консоль в качестве ответа.
#### В программе использует:
```.get_attribute('disabled')``` Определяет, отключен ли веб-элемент, возвращая значение атрибута disabled, что помогает проверить его доступность перед взаимодействием.
```.clear()```  Используется для удаления содержимого веб-элемента, такого как поле ввода.

## Тестовые данные test_E
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет элемент с ожидыаемым числом в нем.
- [x] Обновляет страницу пока в элементе не появися число.
- [x] Если число появиось, выводит его в качестве ответа в консоль.
#### В программе использует:
```while True:``` Выполняет действие пока не появится текст в элементе.
```    driver.refresh()``` Обновляет страциу 
```break``` Прерывает цикл если при обновлении страницы в элементе появился текст.

## Тестовые данные test_F
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет элементы с текстом.
- [x] Если в тексте у элемениов есть число, то суммирует найденные значения.
- [x] Выводит сумму чимсел в качестве ответа в консоль.
#### В программе использует:
```total_sum = 0``` Переменная total_sum, чтобы начать суммирование найденых чисел с нуля.
```number = int(element.text)``` Преобразует текст элемента в целое число и сохраняет в переменную number.
```total_sum += number``` Если преобразование прошло успешно, добавляет значение number к total_sum.
```continue``` Если текст элемента не является числом, пропускает его и продолжаеn цикл.
            
## Тестовые данные test_G
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет элемент с id="this_pic".
- [x] Сохраняет скриншот элемента в папке проекта.
#### В программе использует:
```driver.screenshot('file_name.png')``` Сохраняет скриншот в папке проекта.

## Тестовые данные test_H
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет поле для выбора даты.
- [x] Удаляет текущую дату
- [x] Создает локатор для поискка даты на 10 дней вперед от текущей
- [x] Устанавливает дату по локатору
#### В программе использует:
````import datetime``` # Модуль  для работы с датами и временем
Создает локатор для выбора элемента на 10 дней вперед от текущей даты
```locator_new_date = f"//div[@aria-label='Choose {weekday}, {month} {day}{day_suffix}, {year}']"``` Локатор предназначен для выбора виджета даты, основанного на дне недели, месяце, особом формате дня с суффиксом и годе.


## Тестовые данные test_H1
- [x] Незарегестированный пользователь переходит по ссылке 
- [x] Ищет поле для выбора даты.
- [x] Удаляет текущую дату
- [x] Создает локатор для поискка даты на 10 дней вперед от текущей с помощью библиотеки datetime
#### В программе использует:
```import datetime``` # Модуль  для работы с датами и временем
```now_date = datetime.datetime.utcnow()``` Получает текущую дату
```future_date = now_date + datetime.timedelta(days=10)``` Добавляет 10 дней к текущей
```formatted_future_date = future_date.strftime("%m,%d,%Y")``` Форматирует дату в нужный формат
